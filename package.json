{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "1.0.0-beta.124"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o dist",
    "test": "redocly lint",
    "preview": "redocly preview-docs dist.yaml",
    "prepare": "redocly bundle dist.yaml -o docs/dist.json"
  }
}
